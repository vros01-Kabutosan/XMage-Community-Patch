# Source

This directory will contain the versioned XMage Community Patch source and reproducible build inputs.

The published V6 binaries are treated as reference artifacts only; future changes must be represented here as source or explicit patches.